# include <stdio.h>
int main (){
//int k = 3.0/9;
//printf("%f",k); //float anf int give an float value so 0.33 will be the value of k but demoted to 0 bcoz int is used

//float k = 3.0/9;
//printf("%f",k); // it should return 0.333333 bcoz %f is used for floating output number which is true
return 0;
}