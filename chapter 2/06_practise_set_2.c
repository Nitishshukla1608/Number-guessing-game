#include <stdio.h>
int main()
{
    // int a ; b=a;   this ios not valid bcoz b is not declare
    // int a,b=a;        this is true
    // int a; int b= a;  this is true
    //  int v = 4^3;  this is valid in c but it doeanot work as a exponential operator it act as a bitwise XOR operator
    //  printf("%d",v);      in bitwise 4^3 is 7.
    // char dt = '23'; throughs error only one character is allowed in single inverted comma
    // printf("%f",3.0/8-2);
    // float d = (3.0/8-2);
    // printf("%d\n",d);
    // printf("%f",d);

    // whether the number is divisible by 97 ornot
    // int a;
    // printf("enter the number");
    // scanf("%d", &a);
    // printf("divisiblity test returnr: %d\n", a % 97);
  

// int x=2,y=3,z=3,k=1;
// int result = 3*x/y-z+k;
// printf("the value of the expression is %d",result);

// 3.0+1;  it is a floating point number

    return 0;
}