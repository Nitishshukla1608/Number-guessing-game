#include <stdio.h>
int main()
{
    // int b, c, a = 4; // type declaration instruction
    // b = c = a;       // type declaration instruction
    // printf("the value of a is %d\n\n", a);
    // printf("the value of b is %d\n", b);
    // printf("the value of c is %d\n", c);

    // float b = a + 8.9;         //  bcoz a is not declared first
    // float a = 1.1;


    //float b = 8.5;                                
    //printf("the value of b is %d\n", b);         it returns 0
    
   // float b = 8.5;
   // printf("the value of b is %d\n", b);          it returns 0.000000 

   //jis type ki value hai usi type ka format specifier use karo aur usi type ke datatype  mein use save  karo

    return 0;
}
