#include <stdio.h>
int main()
{
    // question no.1

    // int a = 10;
    // if (a = 11)   // agar turant baads execution block likhte hain to curly braces ki jaroorat nhi hai lekin sirf -
    //                  //ek line of execution ke liye
    // // non zero value is always true in case of assignment operator
    //     printf("i am 11");

    // else
    //     printf(" i am not 11");

    // question no.2

    // int a, b, c;
    // printf("enter the marks in physics ");
    // scanf("%d", &a);
    // printf("enter the marks in chemistry ");
    // scanf("%d", &b);
    // printf("enter the marks in maths ");
    // scanf("%d", &c);
    // float per;
    // per = (a + b + c)/3;
    // if ((per < 40) || a < 33 || b < 33 || c < 33)
    // {
    //     printf("you are fail bcoz total percent is %f or you have gained less than 33 in any subject to GAAND MARAAO", per);
    // }

    // else
    // {
    //     printf("you are pass bcoz total percent is %f ", per);
    // }
    // is problem me ho sakta hai ki total to 40 ke oopar ho par  kisi ek subject mein 33 se kam ho

    // question no.3

    // int a;   // income
    // float b; // tax
    // printf("enter your income : ");
    // scanf("%d", &a);
    // if (a >= 250000 && a <= 500000)
    // {
    //     printf("your tax to be paid is %f\n", (a - 250000) * 0.05);
    // }
    // else if (a >= 500000 && a <= 1000000)
    // {
    //     printf("your tax to be paid is %f\n", (a - 500000) * 0.20);
    // }
    // else if (a > 1000000)
    // {
    //     printf("your tax to be paid is %f\n", (a - 1000000) * 0.30);
    // }
    // printf("THANKING YOU");

    // question no.4

    // int year;
    // printf("enter the year : ");
    // scanf("%d", &year);
    
    // if (year % 4 == 0)
    // {
    //     if (year % 400 == 0)
    //     {
    //         if (year % 100 == 0)
    //         {
    //             printf(" this tear is leap year");
    //         }
    //     }
    // }
    // else
    // {
    //     printf("this year is not a leap year");
    // }

    // question no.6

    // int a,b,c,d;
    // printf("enter the number : ");
    // scanf("%d",&a);
    //  printf("enter the number : ");
    // scanf("%d",&b);
    //  printf("enter the number : ");
    // scanf("%d",&c);


    // question no.5

    // char a;
    // printf("enter the alphabet : ");
    // scanf("%c", &a);
    // if (a >= 65 && a <= 90)
    // {
    //     printf("the alphabet is in UPPERCASE");
    // }
    // else
    // {
    //     printf("this is alphabet is in lowercase");
    // }
    return 0;
}
