#include <stdio.h>
int main()
{
    // int age;
    // printf("enter the age : ");
    // scanf("%d", &age);

    // if (age <= 70 && age >= 18 || (age == 14)) // = ((age <= 70 && age >= 18) || age==14) both are true here
    // {
    //     printf("you are above 18 and below ,70 you can drive \n");
    // }
    // else
    // {
    //     printf("you cannot drive\n");
    // }



     //  if (age <= 70 && age >= 18 || !(age == 14)) in this condition  the person of age 14 cannot drive the 
     //  vehicle bcoz logical not is used before it
// !(3==3) it returns false
// !(3>30) it return true



    return 0;
}